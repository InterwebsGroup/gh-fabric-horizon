{{ 'base.css' | asset_url | stylesheet_tag: preload: true }}
{%- comment -%} Styles inlined to eliminate render-blocking request (~480ms) {%- endcomment -%}
<style>
  @media screen and (min-width: 750px) {
    .section-narrow { max-width: 900px; margin-inline: auto; width: 100%; }
  }
  .accordion .details-content,
  .accordion .text-block,
  .accordion .rte {
    font-size: var(--font-size--md);
  }
</style>
